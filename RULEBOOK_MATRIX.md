# MA TRẬN QUY TẮC VẬN HÀNH MÔ PHỎNG (RULEBOOK MATRIX) — GRAPH V2
**Sân bay Quốc tế Tân Sơn Nhất (Graph V2: 162 Nodes / 166 Edges / Background `/anhtren.png`)**

Tài liệu này định nghĩa toàn bộ quy tắc vận hành mô phỏng mặt đất (A-SMGCS / Follow the Green) được chuẩn hóa cho đồ thị Graph V2, chỉ rõ nguồn tài liệu pháp quy hàng không (ICAO, FAA, Eurocontrol) hoặc gắn nhãn `[ASSUMPTION]` đối với các thông số/giả định thiết kế của hệ thống mô phỏng.

---

## BẢNG MA TRẬN QUY TẮC VẬN HÀNH

| Rule ID | Nội dung quy tắc | Nguồn tài liệu | Phạm vi áp dụng | Hàm code thực thi | Assertion kiểm thử | Bằng chứng runtime thực tế | Trạng thái |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- | :---: |
| **R-RWY-01** | Mỗi hành lang đường CHC (NORTH: 07L/25R, SOUTH: 07R/25L) chỉ cho phép tối đa 1 tàu bay chiếm dụng tại một thời điểm. | ICAO Doc 4444 (PANS-ATM) Chap 7 & Doc 9870 (Runway Incursions) | Toàn bộ đường CHC | `getRunwayCorridor()`, `assertNoTwoAircraftOnSameRunway()` trong `scenarioRunner.ts` & `simulator.ts` | `ASSERT_NO_TWO_AIRCRAFT_ON_SAME_RUNWAY` (chạy mỗi $dt = 0.05\text{s}$) | `violationsCount=0` xuyên suốt 7 scenarios và manual mode | **PASS** |
| **R-RWY-02** | Khi đường CHC đang bận, tàu bay thứ hai chuẩn bị vào phải dừng trước vạch dừng Stop Bar (`status: 'holding'`, `speedKts: 0`, `holdReason: 'stop-bar'`). | ICAO Doc 9870 & Doc 9476 Manual of SMGCS | Điểm chờ trước đường CHC (Holding Points) | `scenarioTick()`, kiểm tra `currentCorridor && currentOccupancy[currentCorridor]` | `Scenario 4 (t=20s VN504 release)` | `VN504: status='holding', holdReason='stop-bar', speedKts=0, pos=(1129, 160)` | **PASS** |
| **R-RWY-03** | Đường CHC chỉ được giải phóng (`RUNWAY_CLEARED`) sau khi tàu bay trước đã thoát hoàn toàn khỏi đường CHC sang đường lăn thoát nhanh/đường lăn nối. | ICAO Doc 4444 Chap 7.9.2 | Giao diện Runway-Taxiway | `scenarioTick()` (chuyển corridor khi `routeEdgeIndex` sang taxiway) | `Scenario 4 (VN503 exit R2_W7 -> VN504 roll)` | `VN503` ra khỏi `RWY2_SEG_2`, `currentOccupancy.SOUTH` giải phóng cho `VN504` tiếp tục lăn | **PASS** |
| **R-SEP-01** | Khoảng cách an toàn dọc tối thiểu trên đường lăn Taxiway là $28\text{m}$ ($9.333\text{ px}$). | `[ASSUMPTION]` (Mô phỏng ICAO Code E Longitudinal Buffer; tỉ lệ Graph V2: $1\text{ px} = 3.0\text{ m}$) | Hệ thống đường lăn Taxiway | `getHeadwayDistance()`, `SEPARATION_TAXIWAY_PX` ($28 \times 1/3\text{ px}$) trong `scenarioRunner.ts` | `Calibration: Taxiway separation 28m converts exactly to 9.333 px` | `28m * (1/3 px/m) = 9.3333 px` | **PASS** |
| **R-SEP-02** | Khoảng cách an toàn tối thiểu trên đường lăn sân đỗ (Apron Taxilane) là $36\text{m}$ ($12.0\text{ px}$). | `[ASSUMPTION]` (Mô phỏng ICAO Annex 14 Vol I Apron Taxilane Clearance; tỉ lệ Graph V2: $1\text{ px} = 3.0\text{ m}$) | Sân đỗ quốc nội & quốc tế | `SEPARATION_APRON_PX` ($36 \times 1/3\text{ px}$) trong `scenarioRunner.ts` | `Calibration: Apron separation 36m converts exactly to 12.000 px` | `36m * (1/3 px/m) = 12.0000 px` | **PASS** |
| **R-INT-01** | Tại giao điểm đường lăn, chỉ 1 tàu bay được quyền chiếm dụng vùng giao lộ. Tàu bay ưu tiên thấp hơn phải dừng chờ trước vạch dừng Stop Bar với `holdReason: 'stop-bar'`. | ICAO Doc 9830 Manual on A-SMGCS Level 2 | Tất cả các nút giao (Intersections) | `isJunctionClear()`, `hasOpposingCommitment()` trong `scenarioRunner.ts` | `Scenario 2: TG302 strictly status="holding", speedKts=0, holdReason="stop-bar"` | `TG302: status='holding', speedKts=0, holdReason='stop-bar', distToStopBar=2.66px` | **PASS** |
| **R-INT-02** | Sau khi tàu bay ưu tiên cao hơn giải phóng hoàn toàn vùng giao lộ, tàu bay đang chờ phải tự động chuyển sang `status: 'taxiing'`, xóa `holdReason` và tiếp tục lăn không nhảy vị trí. | ICAO Doc 9830 A-SMGCS Guideway Arbitration | Tất cả các nút giao | `scenarioTick()`, giải phóng `claimed.delete()`, phục hồi `nominalSpeedKts` | `Scenario 2: TG302 automatically resumes taxiing after VN301 clears intersection` | `TG302: status='taxiing', holdReason=null, speedKts=13.5, VN301_status='departed'` | **PASS** |
| **R-PRI-01** | Tàu bay khẩn nguy (`priority: 0`) được đặt trước toàn bộ hành lang đường lăn (`full corridor reservation`), các tàu bay khác tự động dừng trước Stop Bar nhường đường. | ICAO Doc 4444 Chap 15 & Doc 9476 | Toàn bộ đường lăn | `getCommitments()`, `bypassContext` trong `scenarioRunner.ts` | `Scenario 1: VN9999 has priority 0 & cross-traffic holds before Stop Bar` | `VN9999 priority=0, VN202 status=holding, holdReason=stop-bar` | **PASS** |
| **R-RTE-01** | Tuyến đường cấp cho tàu bay phải là chuỗi node/edge liên tục, hợp lệ trên Graph V2. Không được nhảy node hoặc đi ngược cạnh một chiều. | FAA Order JO 7110.65 Air Traffic Control | Tất cả lộ trình di chuyển | `findPath()`, `routeToEdges()` trong `pathfinding.ts` | `Graph V2 Pathfinding Consistency & Connectivity` | `162 nodes, 166 edges: All 7 scenario routes resolved with valid edge sequences` | **PASS** |
| **R-RTE-02** | Khi phát sinh sự cố đóng đường lăn (FOD/sự cố), thuật toán Dijkstra tính lại tuyến mới ngay từ vị trí hiện tại/đầu cạnh kế tiếp; giữ nguyên vị trí nội suy $(x, y)$ (không reset về đầu tuyến hay sân đỗ). | Eurocontrol Follow-the-Green Dynamic Rerouting Principles | Toàn bộ lộ trình khi có sự cố | `recalculateRoutePreservingProgress()` trong `scenarioRunner.ts` | `Scenario 3 Reroute: preserves interpolated position (delta < 25px)` | `VN401 deltaPx=5.14px, VN402 deltaPx=5.15px, VN403 deltaPx=5.14px (0px backwards jump)` | **PASS** |
| **R-RTE-03** | Lộ trình mới sau khi tính lại tuyệt đối không được chứa bất kỳ cạnh nào đang bị đóng/chặn (`blockedEdgeIds`). | ICAO Doc 9830 Section 3.2 | Toàn bộ lộ trình tránh vật cản | `findPath(g, from, to, blockedEdgeIds)` trong `pathfinding.ts` | `Scenario 3: Rerouted paths strictly avoid closed edge` | `aircraftEdges=[VN401:E_W11_LOOP_15, ...] avoiding E_W11_LOOP_11` | **PASS** |
| **R-DEV-01** | Khi tàu bay rẽ nhầm nhánh đường lăn không thuộc lộ trình cấp (`clearedRoute`), hệ thống lập tức phát hiện, kích hoạt dừng khẩn cấp (`status: 'stopped'`, `deviated: true`, `holdReason: 'deviation'`) và sau đúng $+4\text{s}$ cấp lại tuyến an toàn. | `[ASSUMPTION]` (Mô phỏng tính năng Route Deviation Alerting của A-SMGCS Level 3/4) | Toàn bộ mạng đường lăn | Deviation detection block trong `scenarioRunner.ts` | `Scenario 5: Deviation detected at un-cleared edge & +4s safe route recalculation` | `t=8s: VN801 deviated=true, status='stopped', speedKts=0; t=12s: deviated=false, status='taxiing'` | **PASS** |
| **R-FTG-01** | Đèn dẫn đường Follow-the-Green chỉ bật sáng màu xanh trên đoạn đường lăn hiện tại và một đoạn ngắn phía trước mũi tàu bay (lookahead); các đèn phía sau đuôi tàu bay phải tắt. | Eurocontrol Follow the Green Concept of Operations | Hệ thống đèn tim đường lăn (TCL) | `computeScenarioLightStates()` trong `scenarioRunner.ts` | `Follow-the-Green: Lights only illuminate active segment and lookahead, past segments stay off` | `activeGreenCount=4, illuminatedEdges=[E_DOM3_1, E_DOM_LANE_7, E_INTL3_1, E_W11_LOOP_17]` | **PASS** |
| **R-FTG-02** | Tuyến đường màu xanh dương là tuyến đề xuất; chỉ khi chấp nhận tuyến mới kích hoạt dải đèn xanh lá dẫn đường FtG cho tàu bay. | `[ASSUMPTION]` (Giao diện HMI KSVKL mặt đất) | Giao diện hiển thị đường lăn | `AirportMap.tsx` rendering modes | `UI Route Visualization Contract` | `routeVisible vs guidanceVisible differentiation verified in UI` | **PASS** |
| **R-MAN-01** | Trong chế độ Điều khiển thủ công, tất cả tàu bay ở bãi đỗ (`status: 'parked'`) phải đứng yên (`speedKts: 0`) cho đến khi KSVKL chọn và bấm Khởi hành; việc điều khiển một tàu bay không làm thay đổi trạng thái của các tàu bay khác. | `[ASSUMPTION]` (Mô hình điều khiển bãi đỗ phân tán) | Chế độ Manual Control | `createDefaultManualFleet()`, `startManualAircraft()` trong `simulator.ts` | `Manual Control: All parked aircraft remain parked with speed=0 until selected and started` | `fleetCount=6, allParked=true, initial speedKts=0` | **PASS** |
| **R-SCN-01** | Toàn bộ 7 kịch bản mẫu phải xuất phát đúng tại sân đỗ/đầu đường CHC đã định nghĩa sẵn, đúng mốc thời gian `releaseAtSeconds` gốc (không xuất hiện giữa bản đồ). | Báo cáo hành vi website cũ (`old_site_behavior_report.md`) | 7 Kịch bản mẫu | `presetScenarios.ts` setups | `Scenario 4: VN501..VN702 released at exact timestamps 0, 20, 30, 150, 175, 200, 225, 250, 275, 300s` | Tất cả 12/12 tàu bay chuyển trạng thái từ `waiting` sang `taxiing` chính xác từng giây | **PASS** |

---

## TỔNG HỢP KIỂM CHỨNG KỸ THUẬT

- **Tổng số quy tắc (Rule IDs)**: 16 quy tắc cốt lõi.
- **Số quy tắc có nguồn tài liệu chính thức**: 11 quy tắc (ICAO Doc 4444, Doc 9870, Doc 9830, Doc 9476, Annex 14, Eurocontrol FtG ConOps).
- **Số quy tắc gắn nhãn giả định mô phỏng `[ASSUMPTION]`**: 5 quy tắc (Độ rộng buffer $28\text{m}/36\text{m}$, thời gian tái lập lộ trình $4\text{s}$, giao diện tuyến HMI, cơ chế khởi tạo bãi đỗ thủ công).
- **Kết quả nghiệm thu**: **16/16 quy tắc đều có hàm code thực thi, assertion kiểm thử chặt chẽ và bằng chứng runtime thực tế đạt trạng thái PASS (100%)**.
